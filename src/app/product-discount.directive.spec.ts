import { ProductDiscountDirective } from './product-discount.directive';

describe('ProductDiscountDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductDiscountDirective();
    expect(directive).toBeTruthy();
  });
});
